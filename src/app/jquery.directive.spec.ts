import { JqueryDirective } from './jquery.directive';

describe('JqueryDirective', () => {
  it('should create an instance', () => {
    const directive = new JqueryDirective();
    expect(directive).toBeTruthy();
  });
});
